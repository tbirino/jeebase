describe('user login', function() {
  'use strict';
  it('ensures user can log in', function() {
    expect(1).toBe(1);
  });
  it('ensures path has changed', function() {
    expect(1).toBe(1);
  });

});